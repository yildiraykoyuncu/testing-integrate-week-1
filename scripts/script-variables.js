'use strict';

// found is declared with let because it stores primitives
//  to change it's value we need to reassign it's value
let found = null;

// inputs is declared with const because it stores an array
//  arrays are a reference type
//  updating an entry in the array does not reassign the variable
const inputsArray = [];
